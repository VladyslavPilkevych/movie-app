import React from 'react';
import {
  PageFooter,
} from './Footer.style';

const Footer: React.FC = () => {
  return (
    <PageFooter>
    </PageFooter>
  );
}; // todo

export default React.memo(Footer);
